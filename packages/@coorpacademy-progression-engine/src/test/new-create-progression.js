// @flow
import test from 'ava';
import {newCreateProgression} from '../create-progression';

test('should...', t => {
  // TODO implement
  // $FlowFixMe
  t.is(newCreateProgression(null, null, {}), null);
});
