import {Props} from '../../index.native';

type Fixture = {props: Props};

const fixture: Fixture = {
  props: {
    type: 'close',
    color:'#000',
    isFloating: false
  }
};

export default fixture;
