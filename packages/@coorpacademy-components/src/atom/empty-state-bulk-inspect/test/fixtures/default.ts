import {Fixture} from '../../types';

const fixture: Fixture = {
  props: {
    firstMessage: 'There’s nothing here',
    secondMessage: 'No content tagged as success'
  }
};

export default fixture;
