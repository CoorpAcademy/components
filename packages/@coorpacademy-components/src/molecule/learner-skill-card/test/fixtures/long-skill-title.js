import defaultProps from './default';

const longTitleProps = {
  ...defaultProps.props,
  title: 'Effective Communication, Team Collaboration & Strong Problem-Solving Mindset'
};

export default {
  props: longTitleProps
};
