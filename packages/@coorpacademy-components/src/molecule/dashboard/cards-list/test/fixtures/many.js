import Card from '../../../../card/test/fixtures/default';

const {props} = Card;

export default {
  props: {
    title: 'Most Recent',
    cards: [
      props, props, props, props, props, props, props
    ]
  }
};
