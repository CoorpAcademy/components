import Slide from '../../../setup-cohort-item/test/fixtures/create-new';

const {props} = Slide;
export default {
  props: {
    tabNew: [
      {
        title: 'Create a new cohort'
      }
    ],
    slidesNew: [props]
  }
};
