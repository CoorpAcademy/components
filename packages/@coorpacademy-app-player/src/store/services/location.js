export const exit = () => {
  window.location.reload();
};

export const retry = () => {
  window.location.reload();
};

export const back = content => {
  window.location.reload();
};

export const nextLevel = () => {
  window.location.reload();
};

export const seeComment = () => {
  window.location.reload();
};
