export const exit = () => {
  window.location.reload();
};

export const retry = () => {
  window.location.reload();
};

export const back = (disciplineRef, levelName) => {
  window.location.reload();
};
