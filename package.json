{
  "name": "6poll-front",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "demo": "webpack-dev-server --content-base demo --config demo/webpack.config.js",
    "build": "npm run build:commonjs & npm run build:browser & wait",
    "build:commonjs": "npm run clean:commonjs && babel src --out-dir lib -s",
    "build:browser": "npm run clean:browser && webpack",
    "clean": "npm run clean:commonjs & npm run clean:browser & wait",
    "clean:commonjs": "rm -rf lib",
    "clean:browser": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.2.0",
    "babel-core": "^6.2.1",
    "babel-loader": "^6.2.0",
    "babel-preset-es2015": "^6.1.18",
    "babel-preset-react": "^6.1.18",
    "css-loader": "^0.23.0",
    "extract-text-webpack-plugin": "^0.9.1",
    "pure-css": "^1.0.4",
    "react": "^0.14.3",
    "react-dom": "^0.14.3",
    "react-hot-loader": "^1.3.0",
    "st": "^1.0.0",
    "style-loader": "^0.13.0",
    "stylus-loader": "^1.4.2",
    "webpack": "^1.12.9",
    "webpack-dev-server": "^1.14.0"
  }
}
