import React from 'react';
import { render } from 'react-dom';
import { Button } from '../../src/DumbComponents';

render((
  <div>
    <Button>Yoloa</Button>
  </div>
), document.getElementById('app'));
